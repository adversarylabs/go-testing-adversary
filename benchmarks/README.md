# Calibration corpus

`corpus.json` indexes 50–100 established Go repositories used to study real engineering patterns, calibrate confidence, and audit high-confidence false positives.

The index is metadata only. Upstream source is never copied into or shipped with this adversary. Calibration runs should record immutable commit SHAs separately so results are reproducible while the catalog can continue tracking active projects.
